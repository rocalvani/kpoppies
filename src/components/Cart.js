import handleclick from './utils'

const Cart = () => {

    return (
        <div>
            <p>carrito</p>
    <button onClick={handleclick()}>boo</button>
        </div>
    )
}

export default Cart;